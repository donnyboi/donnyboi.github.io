pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r *.html *.json *.js *.png *.svg public/ 2>/dev/null || true
    - cp -r kid-goals public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
